<!DOCTYPE html>
<html>
  {% include 'head.tmpl' %}

  <body>
    {% include 'header.tmpl' %}

    <main>
      <div class="auth hidden" id="auth">
        <i id="close" class="fas fa-times"></i>
        <div id="wrapper"></div>
      </div>
      <div class="main_wrapper">
        <div class="add_form main_form" id="add_post_desc">
          <h4 class="form_header">Войти</h4>
          <span class="login_info">{{info}}</span>
          <form action="user.php?login=1" method="POST" class="auth_form">
            <p>
              <input type="text" class="inp inp2" name="login" placeholder="Введите Ваш логин">
              <input type="password" class="inp inp2" name="pass" placeholder="Введите Ваш пароль">
            </p>
            <p>
              <a class="main_form_link" href="main_controller.php?retrivePass=1">Забыли пароль?</a>
            </p>
            <button type="submit" class="butn popup_butn">Войти</button>
          </p>
        </form>
      </div>


    </div>
  </body>
</html></main>{% include 'footer.tmpl' %}<script src="public/app_other.js"></script></body></html>
